spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.show-sql=true
spring.datasource.url=jdbc:postgresql://localhost:5432/new?useSSL=false
spring.datasource.username=postgres
spring.datasource.password=sushil123
spring.datasource.initialize=true
server.error.include-message=always

#upoadDir=/home/spacex/Desktop/springbootimage
## File Storage Properties
# Please change this to the path where you want the uploaded files to be stored.
file.upload-dir=/home/spacex/Desktop/springbootimage

# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
#spring.servlet.multipart.max-request-size=215MB
#for liquibase database migration
#spring.liquibase.enabled=true
#spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.xml
